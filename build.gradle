plugins {
    id 'java'
}

repositories {
    jcenter()
}

dependencies {
    testImplementation group: 'org.testng', name: 'testng', version: '6.14.3'
}

tasks.withType(Test) {
    useTestNG()
    testLogging {
        exceptionFormat "full"
    }
}